/**
 * Backwards-compat re-export — the canonical content lives in
 * `src/data/index.ts` and the category files under `src/data/`.
 *
 * Existing imports of `'../data/demo'` continue to work.
 */
export * from './index.js';
